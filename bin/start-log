#!/bin/bash

# start-log - A script to start the logging processes

# get current time
RIGHT_NOW=$(date +"%Y-%m-%d_%H:%M:%S")

# start gpxlogger
filename=~/logs/gpx/$RIGHT_NOW.gpx
gpxlogger -d -f $filename
echo "Started gpxlogger process with output $filename, use stop-log to stop it"
