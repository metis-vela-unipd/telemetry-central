#!/bin/bash

# startlog - A script to start the logging processes

# Get current time
NOW=$(date +"%Y-%m-%d_%H:%M:%S")

# Kill all running process
pkill -9 -f gpxlogger

# Start gpxlogger
mkdir -p ~/telemetry-logs/gpx
filename=~/telemetry-logs/gpx/$NOW.gpx
gpxlogger -d -f $filename
